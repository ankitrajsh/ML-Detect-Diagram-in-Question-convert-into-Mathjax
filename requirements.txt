# Core ML and NLP libraries
transformers==4.41.2
torch>=2.5.0,<3.0
torchvision>=0.21.0,<0.24.0

# API framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Image processing
opencv-python-headless==4.10.0.82
Pillow==11.3.0

# Data processing
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0

# OCR (optional)
pytesseract==0.3.10

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0

# Utilities
python-multipart==0.0.9
aiofiles==23.2.0
pydantic==2.7.1
 
# Apps
streamlit==1.36.0
openai>=1.30.0

# Notes:
# - If you need CUDA support, use the PyTorch selector at https://pytorch.org/get-started/locally/ to get the
#   correct install command (it may use a custom --index-url). Example CPU-only install:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
# - After updating this file, run:
#     pip install --upgrade pip setuptools wheel
#     pip install -r requirements.txt
